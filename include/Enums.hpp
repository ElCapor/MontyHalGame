/*
Storing the enums of the project here to access easier
*/

#ifndef ENUMS_HPP
#define ENUMS_HPP

enum DoorState
{
    STATE_CLOSED,
    STATE_OPENED
};

enum DoorStorage
{
    STORAGE_ZONK,
    STORAGE_CAR
};

enum GameState
{
    GameMainMenu,
    Game
};

#endif